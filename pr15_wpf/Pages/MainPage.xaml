<Page x:Class="pr15_wpf.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pr15_wpf.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      xmlns:conv="clr-namespace:pr15_wpf.Converter"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Основная страница">
    <Page.Resources>
        <conv:StockToBorderConverter x:Key="StockToBorderConverter"/>
        

    </Page.Resources>
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <Label>Поиск</Label>
            <TextBox Text="{Binding searchQuery, UpdateSourceTrigger=PropertyChanged}" TextChanged="TextBox_TextChanged" Name="SearchTextBox" materialDesign:HintAssist.Hint="Введите текст для поиска"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="0">
            <Label FontSize="16">Сортировка</Label>
            <ComboBox SelectionChanged="ComboBox_SelectionChanged">
                <ComboBox.Items>
                    <ComboBoxItem Tag="PriceUp">По цене (↑)</ComboBoxItem>
                    <ComboBoxItem Tag="PriceDown">По цене (↓)</ComboBoxItem>
                    <ComboBoxItem Tag="Name">По наименованию</ComboBoxItem>
                    <ComboBoxItem Tag="CountUp">По кол-ву (↑)</ComboBoxItem>
                    <ComboBoxItem Tag="CountDown">По кол-ву (↓)</ComboBoxItem>
                </ComboBox.Items>
            </ComboBox>
            <Label>Фильтры</Label>
            <StackPanel>
                <Label FontSize="16">Фильтр по брендам</Label>
                <ComboBox x:Name="BrandFilter" Margin="10" SelectionChanged="BrandFilter_SelectionChanged">
                    <ComboBoxItem Tag="0">Все бренды</ComboBoxItem>
                </ComboBox>
            </StackPanel>
            <StackPanel>
                <Label FontSize="16">Фильтр по категориям</Label>
                <ComboBox Name="CategoryFilter" Margin="10" SelectionChanged="CategoryFilter_SelectionChanged">
                    <ComboBoxItem Tag="0">Все категории</ComboBoxItem>
                </ComboBox>
            </StackPanel>
            <StackPanel>
                <Label FontSize="16">Фильтр по цене</Label>
                <Label>от</Label>
                <TextBox x:Name="PriceFromTextBox" Margin="10" PreviewTextInput="NumberValidationTextBox" 
                            Text="{Binding filterPriceFrom, UpdateSourceTrigger=PropertyChanged}"
                            TextChanged="TextBox_TextChanged"/>
                <Label>до</Label>
                <TextBox x:Name="PriceToTextBox" Margin="10" PreviewTextInput="NumberValidationTextBox" 
                         Text="{Binding filterPriceTo, UpdateSourceTrigger=PropertyChanged}"
                         TextChanged="TextBox_TextChanged"/>
            </StackPanel>
            <Button Margin="5" Click="ResetFilters_Click">Сбросить</Button>
            <Button Margin="5" Name="ManagerBtn" Click="GoManager">Настроить каталог</Button>
            <Button Margin="5" Click="GoBack">Назад</Button>
        </StackPanel>
      
            <ListView Grid.Row="1" Grid.Column="1" x:Name="ProductsList"
                  ItemsSource="{Binding products}"
                  HorizontalContentAlignment="Stretch"
                  Foreground="#000" Background="White" FontSize="16">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="25" Name="MyBorder" 
         BorderThickness="3" 
         Padding="10" 
         BorderBrush="{Binding Stock, Converter={StaticResource StockToBorderConverter}}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Row="0" Grid.Column="0">
                                    <TextBlock FontSize="20" Text="{Binding Name}"/>
                                    <TextBlock Foreground="Orange" Text="{Binding Rating, StringFormat='⭐{0}'}"/>
                                    <TextBlock>
                  <Run Text="Категория: "/>
                  <Run Text="{Binding Category.Name}"/>
                                    </TextBlock>
                                    <TextBlock>
                 <Run Text="Описание: "/>
                  <Run Text="{Binding Description}"/>
                                    </TextBlock>
                                </StackPanel>
                                <StackPanel Grid.Row="0" Grid.Column="1">
                                    <TextBlock HorizontalAlignment="Right">
                      <Run Text="Цена: "/>
                      <Run Text="{Binding Price}"/>
                                    </TextBlock>
                                    <TextBlock HorizontalAlignment="Right" Margin="0 49 0 0">
                  <Run Text="Остаток: "/>
                  <Run Text="{Binding Stock}"/>
                                    </TextBlock>
                                </StackPanel>
                                <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,10,0,0">
                                    <ItemsControl ItemsSource="{Binding Tags}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Name, StringFormat='#{0} '}" 
                                     Foreground="#555555" 
                                     FontSize="11"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        <WrapPanel Grid.Row="2" Grid.ColumnSpan="2">
            <TextBlock Text="{Binding products.Count, StringFormat='{}Всего записей: {0}'}"/>
            <TextBlock Text="{Binding Items.Count, ElementName=ProductsList, StringFormat='{} Показано: {0}'}"/>
        </WrapPanel>
    </Grid>
</Page>
